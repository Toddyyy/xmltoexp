def test_consistency():
    score_file = "./example_data/..."
    match_file = "path/to/match.txt"
    ...
    score_note_tied_duration = ...
    parsed_duration = ...  # another source of data
    assert score_note_tied_duration == parsed_duration
    assert 1 == 2
